## Appunti riunione 2/09/2026

* oracolo su LVBench su tutto il dataset
* proviamo a vedere se il segnale dei sink è utilizzabile
* Teniamo il gate su entropy
* check con oracle LVBench: verifichiamo se il segnale di entropy si trasferisce su LVBench;trovare la threshold migliore su LVBench.
* anzichè fare uno shift normale cerchiamo di usare il segnale dell'attenzione: scartiamo
  quelli con poca attenzione
* Provare a loggare il valore dei sink per valutare il percentile da usare per filtrare.
* Obiettivo: ricampionare iterativamente su LVBENCH quando entropy troppo bassa
* implementare query_rows: <last_token>
* ottenere i risultati per task type per le strategy visual_prompting.

## Task e commenti

### Nuova run LVBench (qwen3vl2B)

Voglio lanciare una run di qwen3vl2B su LVBench integrale. Nella stessa run voglio misurare vari dati. Run senza sink filtering: prima di filtrare dobbiamo avere delle prove che siano dei sink.

1. I risultati riportati in 260901-tabelle risultati.md mostrano che la strategia di ricampionamento è ancora aperta. In particolare si nota che su LVBench, in cui abbiamo a disposizione un oracolo per la finestra temporale di maggiore interesse, ricampionare all'interno della finestra porta ad un incremento di accuracy di 16 p.p. Spostandomi su LVBench, voglio capire se possibile sfruttare il segnale di attenzione per trovare la finestra in cui ricampionare.Quindi il prossimo esperimento da fare è: campiono all'interno di quella finestra, l'attenzione sulle celle temporali in quella finestra è maggiore? Ovvero voglio verificare che

$$
Pr(a > \tau | window_{T}) ; Pr(a < \tau | window_{F})
$$

	Se valgono, Tuniamo la threshold t, altrimenti il segnale di attenzione non è adatto a trovare la finestra corretta.

2. Calcolare il valore di entropia (gate) delle risposte per ciascuna domanda, in modo da avere una tabella entropia -> risposta corretta sì/no, in modo da separare al meglio. Come faccio a trovare una threshold adatta ai vari dataset?
3. Riportare l'accuracy per task type per LVBench: in questo caso è possibile verificare se ci sono dei task in cui la coppia modello - arm performa meglio.
4. Per i token considerati sink, riportare l'istogramma dei valori sulle dimensioni, con statistiche relative alla media e una heatmap

### Run passata su videoMME

6. Per le run passate in VideoMME, estrarre le statistiche dell'accuracy relativamente ad ogni task-type
7. Riportare in un excel tutti i risultati e i numeri (solo numeri!) ottenuti finora. Nessun commento, solo informazioni chiave come modello - arm - setting- accuracy.
